#pragma once
#include <vector>
std::vector<float> MatrixMultiplication(const std::vector<float>& A,
                                        const std::vector<float>& B, int M,
                                        int N, int K);